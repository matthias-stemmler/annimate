import { ColumnProps } from '@/components/columns/props';
import { FC } from 'react';

export const MatchInContextColumn: FC<ColumnProps<'match_in_context'>> = ({
  data,
  onChange,
}) => {
  data;
  onChange;
  return 'match_in_context';
};
